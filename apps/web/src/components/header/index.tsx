import { ContainerHeader } from "../containter";

export function Header() {
  return (
    <ContainerHeader>
      <nav className="px-2 grid items-center h-[50px]card-description sm:h4 lg:h3 xl:h2 2xl:h1 sm:px-2">
        NavBar
      </nav>
    </ContainerHeader>
  );
}
